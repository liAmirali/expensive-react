import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    allVariants: {
      fontWeight: "lighter",
      fontFamily: "Roboto",
    },
    button: {
      textTransform: "none",
    },
  },
});
